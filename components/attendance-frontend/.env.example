# Example environment variables for running the demo server
PORT=4000
# Optional: path to built frontend (defaults to ../dist)
#DIST_DIR=../dist
PORT=4000
# DIST_DIR is the path the server will use to serve static files. Default is ../dist
DIST_DIR=../dist
